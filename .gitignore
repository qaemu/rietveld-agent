__pycache__/
*.pyc
.venv/
venv/
*.gpx
build/
dist/
*.egg-info/
.pytest_cache/
data/raw/
data/catalog/
*.dmg
.DS_Store
.env.vendor/
# heavy derived/downloaded artifacts (reproducible via the unit scripts)
data/cod_index/
# derived work trees of every unit (reports+inputs are tracked; the rest
# regenerates with `make report` and the unit runners)
data/*/work/
# unit 15 probe work dir (gpx/lst/xye already ignored; prm are derived)
# unit 16 work dir (gpx/lst/xye/prm all derived; results/ is tracked)
*.lst
# paper build intermediates (the compiled PDF is tracked)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
*.xye
.vendor/

# gate run artifacts (results/ is tracked; logs/work are derived)
data/qpa_gate/work/
data/qpa_gate/gate.log
# on-demand COD CIF download pool (rebuilt by the pipeline / cod_full.py)
data/cif_pool/
data/unit12/work/cifs/
data/qpa_gate/sweeps/**/*.gpx
data/qpa_gate/sweeps/**/*.lst
data/qpa_gate/sweeps/**/*.prm
data/qpa_gate/sweeps/**/*.xye
data/qpa_gate/sweeps/**/*.bak0.gpx
data/qpa_gate/sweeps/**/*.json.tmp
