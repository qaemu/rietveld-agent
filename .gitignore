__pycache__/
*.pyc
.venv/
venv/
*.gpx
build/
dist/
*.egg-info/
.pytest_cache/
data/raw/
data/catalog/
*.dmg
.DS_Store
.env.vendor/
# heavy derived/downloaded artifacts (reproducible via the spike scripts)
data/cod_index/
# derived work trees of every spike (reports+inputs are tracked; the rest
# regenerates with `make report` and the spike runners)
data/*/work/
# spike 15 probe work dir (gpx/lst/xye already ignored; prm are derived)
# spike 16 work dir (gpx/lst/xye/prm all derived; results/ is tracked)
*.lst
# paper build intermediates (the compiled PDF is tracked)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
*.xye
.vendor/
