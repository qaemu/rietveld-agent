# Runtime dependencies (pip install -r requirements.txt, or make env)
numpy>=1.24
scipy>=1.10
jsonschema>=4.17
matplotlib>=3.7
pytest>=7
# GSAS-II binary bootstrap (GitHub API + tgz download)
requests>=2.28
# GSAS-II optional importer backends required by the Rietveld engine
PyCifRW>=4.4  # CIF phase/powder importers
xmltodict>=0.13  # Bruker/BRML importer support